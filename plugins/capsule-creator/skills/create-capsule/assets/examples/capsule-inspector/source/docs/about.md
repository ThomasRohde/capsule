# Capsule Inspector

Capsule Inspector is a reference SQLite Capsule that examines another SQLite
file as inert data. It loads the user-selected bytes into the pinned official
`@sqlite.org/sqlite-wasm` engine in browser memory, applies fixed bounded
catalogue queries, and renders results with text-only DOM APIs.

The target application's HTML and JavaScript are never mounted. Its commands,
prompts, runbooks, and declared check SQL are listed but never executed. The
Inspector never writes target bytes and has no network permission.

The four-stage X-ray rail separates file readability, SQLite coherence,
Capsule contract identity, and application inventory. A positive result means
the visible structures are coherent; it is not publisher authentication and
does not replace the independent verifier before execution.
