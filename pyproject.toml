[project]
name = "sqlite-capsule-lab"
version = "0.4.3"
description = "Reference implementation for self-describing applications packaged in SQLite"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
